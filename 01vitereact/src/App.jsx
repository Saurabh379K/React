
function App() {
  const username = "Saurabh"

  return (
    <h1>react with vite by {username}</h1> // this is called evaluated expression
    // We can't write code inside the evaluated expresion we just write the final outcome
    // The JS is written before the return statement and the fianl answer is written in the evaluated expression
  )
}

export default App
